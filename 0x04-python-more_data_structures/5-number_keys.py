#!/usr/bin/python3
# 5-number_keys.py


def number_keys(a_dictionary):
    """Return number of keys in dictionary."""
    sum = 0
    keys_list = list(a_dictionary.keys())

    for i in keys_list:
        sum += 1

        return (sum)
